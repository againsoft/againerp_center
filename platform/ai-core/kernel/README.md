# AI Core — Kernel

**Owner:** AgainERP Center (`platform/ai-core/kernel/`)  
**Consumer:** `platform/ai-core/` internal modules only

Platform AI kernel — lifecycle entry, core loop, and module bootstrap for AI Core. MoharazNX must never import.

**Migration:** Phase 3 — extract from `apps/api/app/services/ai_service.py`
