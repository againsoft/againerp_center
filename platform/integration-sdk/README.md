# Integration SDK

**Status:** ⬜ Scaffold  
**npm name:** `@againerp/integration-sdk` (planned)

Third-party integration contracts and adapter interfaces — webhooks, OAuth connectors, external service bridges.

Center defines contracts; MoharazNX implements business-facing integration config only.

**Does NOT contain:** Provider Gateway (LLM) — that is `platform/provider-gateway/`.
